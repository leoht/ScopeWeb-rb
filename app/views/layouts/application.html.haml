!!! 5
%html
	%head
		%title SCOPE | L'oeil ouvert sur le monde
		= stylesheet_link_tag    "application", media: "all"
		= javascript_include_tag "application"
		= csrf_meta_tags
		= yield :head

	%body
		%nav.navbar
			%a{ href: "/", class: "nav-scope with-font"}
				SC
				%span.blue> 0
				PE

			= link_to 'Les films', find_movies_path(:skip => 1), :class => 'active'

			= link_to 'On trouve pour vous', :find_movies

			= link_to "L'Application", nil

			.current.current-movies

			.nav-search
				%form.search-form{ action: search_movies_path, method: :get }
					%input{ type: :text, name: :s, placeholder: 'Rechercher un film' }
					%img.submit{ src: '/assets/search-btn.png' }
			
			.nav-right
				- if current_user
					= link_to 'Déconnexion', signout_path, id: 'sign_out'
				- else
					= link_to 'Inscription', login_path('target' => request.original_url), id: 'sign_in'


		= yield